echo name: Deploy to GitHub Pages > deploy.yml
echo. >> deploy.yml
echo on: >> deploy.yml
echo   push: >> deploy.yml
echo     branches: [ main ] >> deploy.yml
echo. >> deploy.yml
echo jobs: >> deploy.yml
echo   build-and-deploy: >> deploy.yml
echo     runs-on: ubuntu-latest >> deploy.yml
echo     steps: >> deploy.yml
echo       - uses: actions/checkout@v2 >> deploy.yml
echo       - uses: actions/setup-node@v2 >> deploy.yml
echo         with: >> deploy.yml
echo           node-version: '18' >> deploy.yml
echo. >> deploy.yml
echo       - run: npm ci >> deploy.yml
echo       - run: npm run build >> deploy.yml
echo. >> deploy.yml
echo       - name: Deploy >> deploy.yml
echo         uses: peaceiris/actions-gh-pages@v3 >> deploy.yml
echo         with: >> deploy.yml
echo           github_token: ${{ secrets.GITHUB_TOKEN }} >> deploy.yml
echo           publish_dir: ./dist >> deploy.yml